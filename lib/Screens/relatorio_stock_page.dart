import 'package:flutter/material.dart';

class RelatorioStockPage extends StatelessWidget {
  const RelatorioStockPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("Relatorios"),
    );
  }
}
